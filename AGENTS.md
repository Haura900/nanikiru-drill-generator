# Project completion policy

- When the user requests a bug fix or code change for this project, completion includes appropriate tests, committing the scoped changes, pushing `main`, monitoring the GitHub Pages workflow, and verifying the deployed site.
- Do not stop after a local implementation unless the user explicitly asks not to deploy or deployment is blocked.
- Never include unrelated user changes in a commit. If publishing cannot be completed, report the exact blocker.
